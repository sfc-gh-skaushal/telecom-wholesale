name: 'wholesale_dbt'
version: '1.0.0'
config-version: 2

profile: 'sfseeurope-skaushal'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  wholesale_dbt:
    staging:
      +materialized: view
      +schema: raw_whl
    marts:
      +materialized: table
      +schema: product_whl
